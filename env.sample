# Postely-Core environment sample

# Application
APP_ENV=development
PORT=8080

# PostgreSQL DSN (used by src/config.go)
DATABASE_URL=postgres://user:password@localhost:5432/postely?sslmode=disable

# Optional security/auth (placeholders)
# JWT_SECRET=change_me
# TWO_FACTOR_ISSUER=Postely


